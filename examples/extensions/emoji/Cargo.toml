[package]
name = "emoji"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
portunus-ext-sdk = { path = "../../../extension-sdk" }
serde_json = "1"

[profile.release]
opt-level = "s"
lto = true
